language: python
sudo: required

services:
  - docker

before_install:
  - python setup.py build && pip install -e .
  - docker-compose --verbose up -d
  - sleep 5
  - docker ps
  - ss -lnpt

script:
  - python tests/basic.py
