language: python
sudo: required

services:
  - docker

before_install:
  - python setup.py build && pip install -e .
  - docker-compose up -d
  - sleep 5
  - ls -la db/
  - docker ps
  - ss -lnpt

script:
  - python tests/basic.py
